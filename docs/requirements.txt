# Documentation build dependencies for ReadTheDocs

# Install pydsstools from PyPI (pre-built wheel, can't compile on RTD)
# Note: RTD docs will reflect the latest PyPI release
# Use --pre to include pre-release versions (e.g., 3.0.0b3)
--pre
pydsstools

# Sphinx and extensions
myst-parser>=4.0.1
numpydoc
sphinx>=8.2.3
sphinx-rtd-theme
sphinx-autodoc-typehints
autodoc-pydantic>=2.0.0
