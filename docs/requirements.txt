# Documentation build dependencies for ReadTheDocs

# Install pydsstools from PyPI (pre-built wheel, can't compile on RTD)
# Note: RTD docs will reflect the latest PyPI release
#
# IMPORTANT: Pin NumPy <2 because wheel was built with NumPy 1.x (oldest-supported-numpy)
# RTD would otherwise install NumPy 2.x causing ABI mismatch error:
# "numpy.dtype size changed, may indicate binary incompatibility"
numpy<2
pydsstools

# Sphinx and extensions
myst-parser>=4.0.1
numpydoc
sphinx>=8.2.3
sphinx-rtd-theme
sphinx-autodoc-typehints
autodoc-pydantic>=2.0.0
